# Copy to .env.local and fill in real values (.env.local is gitignored).

# Optional — used for canonical/og:url meta tags. Falls back to window.location.origin.
APP_URL="https://your-domain.vercel.app"

# Required for the contact form — create at https://web3forms.com with recipient Sayyed.vali@gmail.com.
# Also set this in Vercel: Project Settings > Environment Variables (name: VITE_WEB3FORMS_ACCESS_KEY).
VITE_WEB3FORMS_ACCESS_KEY="d117c8a4-0929-4569-a257-4a0ef75bf466"
